export const generateCertificatesForRegistration = async (registration, event) => {
  // TODO — implement certificate generation
  console.log(`📜 Certificate generation triggered for registration: ${registration._id}`);
};